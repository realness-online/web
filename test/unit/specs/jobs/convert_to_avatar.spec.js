describe('@/jobs/convert_to_avatar', () => {
  it('it should covert the image to a square ')
  it('it should resize the image to 128x128px')
  it('it should trace the imgae into an avatar')
  it('it should optimize the avatar') 
})
