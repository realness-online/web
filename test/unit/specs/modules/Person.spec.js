import Person from '@/modules/Person'
describe('Person.js', () => {
  it('Person exists', () => {
    expect(Person).toBeDefined()
  })

  // it creates a new user and stores their activity on the server
  describe('a person', () => {
    it('can create a profile')
    it('has a home page')
    it('has activity')
    it('has a profile image')
    it('has a name')
  })

})
