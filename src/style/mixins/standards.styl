standard-border($color=currentColor, $dark-color=dark-black)
  border: (base-line / 10) solid $color
  border-radius: (base-line / 3)

standard-button($color=currentColor, $dark-color=white)
  standard-border: $color
  padding: (base-line / 2.33)
  border-radius: (base-line / 1.33)
  // border-radius: base-line
  line-height: 1
  background-color: inherit
  vertical-align: middle
  text-transform: capitalize
  text-align: center
  font-weight: 300
  cursor: pointer
  color: $color
  &:disabled
    color: transparent
  @media (prefers-color-scheme: dark)
    color: $dark-color
    standard-button: $dark-color
