standard-border($color=currentColor)
  border: (base-line / 6) solid $color
  border-radius: (base-line / 3)

standard-button($color=currentColor)
  color: $color
  standard-border: $color
  border-radius: base-line
  padding: (base-line / 2)
  cursor: pointer
  text-align: center
  font-weight: normal
