# Realness tooling (curated)

Vue/Vite + WASM + Cloudflare workers site (realness.online), Firebase
hosting/storage. Heavy build + deploy + test/risk tooling. Run scripts from
`work/realness`.

## vite-plus (`vp`)

One toolchain for dev, build, test, lint, and format. Configured in
`vite.config.js` - there is no `.eslintrc` or `.prettierrc`.

- `vp fmt` (format) and `vp lint` (oxlint) are built in. `vp check` runs both.
  `npm run check` reports, `npm run check:fix` writes. There are no separate
  lint/format scripts - `check` is the one entry point.
- `lint.plugins` are oxlint's own rule sets - no npm packages behind them.
- `lint.jsPlugins` loads real ESLint plugins from `node_modules`, so those stay
  devDependencies. `eslint-plugin-compat` is one; dropping it breaks `vp lint`.
- Run project scripts, not `npx vp` - npx can resolve a different vp than the
  project's and skip its config.

## Diagnose / test

- `npm run test:coverage` then `npm run fallow:report` - coverage + refactor-risk health.
- `npm run test:risk` / `test:risk:report` - coverage + fallow + risk report in one.
- `npm run test:risk:report` / `test:coverage:summary` - plaintext risk/summary output.
- `npm run test:fail-fast` - stop at first failure (`--bail=1`).
- `npm run type` - tsc type check. `npm run check:fix` - format plus lint, writes.
- `npm run test:rules` - Firebase storage rules tests (`firebase emulators:exec`).
- `npm run verify` - verify a deploy (manifest vs site).

## Build / deploy

- `npm run build` - full: icons, wasm, toc, prerender, manifest.
- `npm run deploy` - build + firebase deploy + `verify-deploy`.
- `npm run ship` - deploy + gh release + verify.
- `npm run prerender` parts: `build:prerender`, `build:manifest`, `build:toc`, `build:icons`, `build:wasm`, `build:workers`.

## Dev / serve

- Prefer `https://realness.local` (already running) - do not start another
  `npm run dev` for browser/preview work.
- `npm run dev` - only if nothing is serving; runs `generate-toc.js` first.
  With mkcert certs present, Vite serves HTTPS on `realness.local:443`.
- `npm run serve` - preview build. `serve:https` - serve dist.
- `dev:workers` - watch workers.

## Poster pipeline (`poster`, `poster:video`)

Both drive the built app in a headless browser. Failures show up as silence.

- They render `dist/`, not `src/`. Rebuild after changing `src/`, or the run
  waits 120s and reports "poster driver never became ready".
- `poster` needs `CHROME_PATH`. `poster:video` picks a browser itself.
- `poster:video` renders `https://realness.online` unless `REALNESS_URL` is set,
  so it depends on the deploy. Script and deploy must agree on the driver's
  global name - the checkout says `window.poster_driver`, the live deploy still
  says `window.__poster_driver`. Local: `npm run serve`, then
  `REALNESS_URL=https://realness.local:<port>`.
- Devtools calls have no timeout; a lost reply parks the run with no output.
- A killed run leaves headless browsers on ports 9334/9335. The next run
  attaches to those instead of its own and two processes drive one page - the
  "poster never persisted" storm. `pkill -f "user-data-dir=.*poster-"`.
- They use the same Brave you browse with. Ten-ish headless instances wedged it;
  `open -a "Brave Browser"` brings it back.

## Note

- `npm run check` is `vp check` - format plus lint, no tests. It cannot tell you
  a fix works; `npm run pre-commit` can. `docs` is JSDoc.
- `.vite-hooks/pre-commit` runs `npm run pre-commit`, so committing already runs
  the full gate. Its comment is what stops `vp config` appending a `vp staged`
  step - leave it.
- `pre-commit` ends in `test:rules`, which needs port 9299. Another project's
  firebase emulator holding it fails the hook for reasons unrelated to a change.
- `fallow` / `fallow:fix` need a `fallow-report.json`; run `fallow:report` first.
- `og:candidates` / `og:pick` are Open Graph image tooling (rarely needed).
- Deployment uses `verify-deploy.js`; a failed verify means the site drifted from manifest.
