service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    },
    match /people/{phone_number}/ {
      allow write: if request.auth.token.phone_number == phone_number
    }
  }
}
