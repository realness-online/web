# Flesysem sync

I want to enable the filesystem feature on chrome or other supported browserso that all the posters the user has are synced with a local folder. this way they don't have to download everything one poster at a time.
